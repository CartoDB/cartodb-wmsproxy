[loggers]
keys=root
[handlers]
keys=mapproxy

[formatters]
keys=default

[logger_root]
level=INFO
handlers=mapproxy

[handler_mapproxy]
class=FileHandler
formatter=default
args=(r"/var/log/mapproxy/api.log", "a")

[formatter_default]
format=%(asctime)s - %(levelname)s - %(name)s - %(message)s
